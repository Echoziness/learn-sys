# 开发路线图

> **版本**：v1.3 · 2026-08-28（冻结前最后功能落地：追问回归/多域选择/导出纯度；W3 交付物周进行中）
> **文档类型**：Roadmap — 排期与验收的唯一事实源。每周末验收对照本文。
> **总纪律**：一切工作必须落在四类证据物之一——①可视化系统（视频主体）、②指标数字、③测试数据（50 组画像 + 2 组完整示例）、④可部署源码。不能变成证据物的功能一律不做。

---

## 0. 关键日期

| 节点 | 日期 | 内容 |
|---|---|---|
| 功能冻结 | 2026-09-01 | 此后只修缺陷，不加功能 |
| **初审提交** | **2026-09-05** | 邮箱打包提交（学校—姓名—作品—电话 命名） |
| 初审出结果 | 2026-09-20 | 发榜单位完成初审 |
| 终审擂台赛 | 2026-11 | 届时再完善（React Flow 完整版等留到 10 月） |

## 1. 评分映射（100 分 → 我们的证据物）

| 评分项 | 分值 | 证据物 | 产出周 |
|---|---|---|---|
| 完整性：全流程闭环可部署 | 30 | 本地部署说明 + 提交包 + 视频闭环演示（画像→调度→资源） | W1 地基 / W3 交付 |
| 创新性：辩论交叉验证防幻觉 | 25 | claim_type 分层审核 + evidence 溯源链 + 裁判面打回可视化 + **幻觉率 <5% 实测数字** | 机制 ✅ / 数字 W2 / 可视化 W2 |
| 体验：调度可视化 + 资源排版 | 15 | 裁判面（React Flow）+ 学生面 + 报告三图 | W2 |
| 实用价值：测试方案 + 三指标 + 泛化 | 30 | 50 组画像批量评测报告（三指标数字）+ 2 组完整输入输出示例 + 领域可迁移架构论证 | W2 数字 / W3 示例 |

## 2. W1（08-14 → 08-20）｜资源与地基周

**主题**：会话持久化 + 资源沉淀层 + API 骨架。周末验收 = 用 CLI 跑完一个会话后，能从 DB 查出完整事件流、轮次快照与三形态资源包。

| # | 任务 | 验收标准 | 状态 |
|---|---|---|---|
| 1.1 | DB 迁移：5 张新表（架构文档 §3.2）幂等迁移 | 老库重跑 init_db 无损升级；新表可读写 | ✅ |
| 1.2 | `core/session.py` SessionStore | 事件写入含 seq 单调；读写均有 pytest | ✅ |
| 1.3 | `core/teach_loop.py` 抽取 + run_cli 薄化 | CLI 行为不变（回归：现有测试全绿 + 一次 E2E），会话数据落库 | ✅ |
| 1.4 | `core/deliver.py` 资源组装 | 会话结束后 resource_packages 三形态齐备；讲义含 evidence 溯源 | ✅ |
| 1.5 | 实操指南：generate prompt 对 procedure 条目分支 | 指南含步骤/示例/检查点，受审核（幻觉率口径覆盖它） | ✅ |
| 1.6 | 进阶挑战标记 | mastery≥0.85 的主题 package 带 challenge 字段 | ✅ |
| 1.7 | api/ 骨架：七端点 + SSE（架构文档 §5） | curl 可完成建会话→教学→出题→作答全流程；事件落库 | ✅ |
| 1.8 | docker-compose 骨架 + web/ 脚手架 | compose up 起 api+web；Next.js 15 + shadcn 初始化完成 | ✅ |

**排雷**：BGE-M3 在 api 启动时常驻加载一次；LLM 失败返回结构化错误事件（SSE 不静默断流）。

## 3. W2（08-21 → 08-27）｜证据与体验周

**主题**：指标数字 + 三画面。周末验收 = 三项指标数字产出 + Web 可演示完整闭环。

| # | 任务 | 验收标准 | 状态 |
|---|---|---|---|
| 2.1 | 评测口径落地 `evals/metrics.py`（三指标，PRD §5） | 与 pytest 同口径；每指标可独立复算 | ✅ |
| 2.2 | 50 组画像：3 组手工精设 + 47 组程序生成 | `evals/profiles/` 50 个 JSON；3 组演示画像经人工确认 | ✅ |
| 2.3 | `evals/run.py` 批量评测 | 一条命令产出 JSON 报告：三指标数字 + 明细。**先小批（5 组）验证幻觉率，超标立即调 prompt** | ✅（小批流程已验；教学弧收敛后单会话 3.8%；全量批跑收盘归档） |
| 2.4 | Web 学生面 | 画像输入 → 流式教学 → 答题交互 → 决策反馈 | ✅ |
| 2.5 | Web 裁判面（视频主画面） | React Flow：agent 节点流转 + review 打回回路 + 证据溯源侧栏。**降级预案：时间不足改为事件时间线 + 简图** | ✅ |
| 2.6 | Web 报告页 | Recharts 三图：盲区雷达 / 难度匹配曲线 / 路径图（含 regress 回边） | ✅ |
| 2.7 | 回放模式 | 打开历史会话可完整重放（无 key 演示保障） | ✅ |

## 4. W2+ 插入迭代（08-18 → 08-23）｜教学质量 + 可复用闭环

**主题**：真实会话实测暴露的教学质量问题批量修复 + 赛题"可复用资源"落地。均已验收（pytest 193 全绿 + 真实 LLM 会话验证）。

| # | 内容 | 验收结果 |
|---|---|---|
| 2.8 | 教学质量四项修复：choice 重做为概念辨析题（LLM 三件套 + bigram 校验）/ 讲义全轮次累积（修复只取最后一轮丢内容）/ 教学弧（概念→示例→要点，论断均长 90→173 字）/ choice 反馈覆盖率显示隐藏 | 真实会话验证 ✓；教学弧初版幻觉率回归 7.2%，收窄要点层后 3.8%<5% |
| 2.9 | 产出物条目化导出：distill agent（错题/脚手架 → 误区知识）+ package_to_entry 同构条目 + export_packages.py 自检导出 | p03 会话导出 3 条（5 条真实误区提炼）→ init_db 原样入库 → FTS 命中——可复用闭环全链路通 |

**关键教训**：教学弧的要点层曾引导 LLM 写条目外工程实践（幻觉率 7.2%）——生成端与审核锚点必须同源（第三次同源复发，已入 AGENTS.md 坑表）。审核消融对照数字已确认隐含在事件流（review_done 首轮裁决 vs teach_delivered 最终裁决），收盘聚合即可。

## 5. W3（08-28 → 09-04）｜交付物周

**主题**：提交包组装。09-01 功能冻结。系统冻结后一次性收盘：全量评测归档、本地部署复验、双画像对照演示。
**冻结前最后功能落地（08-27~28）**：导出知识纯度三重过滤 + distill 硬性约束 A-F；追问机制设计回归（记录困惑→解答→下轮注入）；多域会话级选择 + linux-ops 第二领域（18 条，泛化可演示）；测试基线 222。

| # | 任务 | 验收标准 | 状态 |
|---|---|---|---|
| 3.1 | 完整会话示例（测试数据包） | 提交物"完整输入输出示例"：画像 + 中间数据 + 最终资源，齐备（50 组全量已入交付包） | ✅ |
| 3.2 | 10 分钟视频 | 分镜：双画像对比输入(1') → 裁判面协同调度(3') → 教学交互与追问(2') → 三形态资源(2') → 报告三图(1.5') → 部署演示(0.5') | 🔨 分镜已定稿（演示幻灯片设计） |
| 3.3 | 设计实现方案文档 + PPT | 含三指标实测数字、架构图、赛题追溯矩阵；正面回答两论证：审核价值（消融对照数字）、产出物复用（条目化闭环） | ✅ 方案文档（PPT 制作中） |
| 3.4 | 部署说明定稿（本地部署，2026-08-27 放弃 Docker） | 干净环境按 docs/部署说明.md 可走通全流程（含模型放置与 .env 配置，Windows 实测复现过） | ✅ |
| 3.5 | 第二领域切片 + 会话级领域选择 | linux-ops 18 条 + 建会话自选领域（诊断/检索/教学/资源同域隔离，零代码换域）——泛化从论证变为可演示功能 | ✅ |
| 3.6 | 09-05 提交打包 | 命名合规（学校—姓名—作品—电话），材料清单对照赛题第八条勾验 | 🔨 打包脚本就绪（含模型 2.2G），待视频/报名表齐备后终打包 |
| 3.7 | v0.5 版本 tag | 冻结前最后功能全部入库后打点（文档全量同步） | 🔨 |

**收盘顺序**：系统冻结 → 本地部署复验 → 全量 50 组评测（三指标 + 审核消融两层聚合）→ 冻结前功能（追问/多域/导出纯度）→ 视频 → 终打包提交。已完成：部署复验、评测归档、冻结前功能、方案文档；剩：视频、终打包。

## 6. 风险与预案

| 风险 | 概率 | 预案 |
|---|---|---|
| 幻觉率实测超标（>5%） | 中 | W2.3 第一天小批跑；调 generate 证据锚定与 review 严格度，重跑验证 |
| Web 工作量失控 | 高 | 裁判面降级为时间线；报告页三图砍为两图；学生面优先保闭环 |
| LLM 供应商抖动 | 中 | 回放模式兜底（W1 地基 + W2.7）；评测支持断点续跑 |
| 评测跑批时长 | 低 | 50 组 × ~30 调用，并发压到 30 分钟内；deepseek flash 成本可忽略 |
| 会话持久化迁移踩坑 | 低 | 幂等迁移已验证过一次（knowledge_type）；新表全部 CREATE IF NOT EXISTS |

## 7. 每周验收纪律

- 周五晚对照本文验收栏逐项打勾，未达项周六补，周六仍不过则砍范围不延周；
- 验收 = 运行产物（DB 记录 / 指标 JSON / 页面截图），不是"代码写完了"；
- 全程 pytest + pyright + ruff 三绿才算任务完成（现有 120 测试为回归基线）。
