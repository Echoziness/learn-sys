# 开发约束与工程规范

> 状态：强制。本文档是 `技术选型记录.md` 与 `架构设计文档.md` 的执行层，违反文中约束的代码一律驳回。
> 如有正当理由需偏离，必须先修改本文档并注明原因。
> 本文约束同时摘编至仓库根目录 `AGENTS.md`（给 AI agent 读的紧凑版），二者冲突时以本文档为准。
> 排期与验收见 `开发路线图.md`（不重复）。

---

## 1. 总则

### 1.1 适用性

- 所有提交到本仓库的代码（含 AI 生成）均须遵守本文约束；
- code review 的唯一检查标准即为本文各条目。

### 1.2 模块化单体原则

本项目的操作模型是一个团队由 AI 为主力 coder 的单人开发。微服务、消息队列、分布式组件在这个模型下是负资产。所有代码必须保持 **模块化单体**：一个 Python 包 + 一个 Next.js 应用 + 一个 SQLite 数据库，再无其他运行态组件。

---

## 2. 技术栈锁定（最高优先级，不可在开发中变更）

| 层 | 锁定 | 版本要求 |
|---|---|---|
| 编排框架 | LangGraph（仅 Python 版，禁止使用 JS 版） | `>=1.0` |
| 后端 | FastAPI + SSE | `>=0.115` |
| 数据库 | SQLite（业务 + FTS5 + sqlite-vec 向量） | FTS5 为 SQLite 内置 |
| Embedding | BGE-M3（sentence-transformers） | 本地，无 API 依赖 |
| LLM 接入 | `core/llm.py` 统一入口：AsyncOpenAI client，用户注入 base_url + api_key | 不预设模型名，不硬编码 provider |
| 前端框架 | Next.js 15 App Router | TypeScript strict |
| 前端样式 | Tailwind CSS v4 + shadcn/ui | 组件必须从 `@/components/ui` 导入 |
| 前端图表 | React Flow（@xyflow/react）+ Recharts | 禁止引入第三方图表库 |
| 交付 | docker compose | api + web + 初始化数据卷 |
| 评测 | pytest | 与工具脚本同源 |
| 环境管理 | uv（Python 依赖）+ pnpm（前端依赖） | 锁文件必提交 |

### 2.1 禁止引入（开发中发现需要以下组件，必须先用非技术方案解决）

- ❌ 消息队列（RabbitMQ / Kafka / Redis Stream / Celery）
- ❌ 时序数据库 / 列存 / OLAP
- ❌ 微服务 / 独立 sidecar
- ❌ NestJS / Express / Bun（后端）
- ❌ Vue / Svelte / Angular（前端）
- ❌ Ant Design / Element UI / Material UI（组件库——shadcn 独占）
- ❌ LangGraph JS（Python 独占）
- ❌ Chroma / LanceDB / Pinecone / Weaviate（向量检索 SQLite 全家桶独揽）
- ❌ Electron（进入缓冲周之前禁止接触）

**新依赖准入流程**：先在本文档 2.1 节申请→注明理由与替代评估→核心拍板→更新本文档→再安装。

---

## 3. 仓库结构（强制布局，与当前代码对齐）

```
learn-sys/
├── core/                    # Python 包：教学内核 + 会话服务（禁止模块级副作用）
│   ├── agents/              # Agent 定义（每个 agent 一个文件）
│   │   ├── diagnose.py      # 学情诊断（gap_ids 收敛到本体目录）
│   │   ├── generate.py      # 领域知识生成（claim_type 分层 + 教学弧 + 错因回流）
│   │   ├── review.py        # 审核裁判（NLI 三分类，rule_check/merge_verdicts 纯函数）
│   │   ├── feedback.py      # LLM 判分复核 + 教学评估（fail-closed + missed 硬降级）
│   │   ├── question.py      # 回答题题干+expected 生成 + choice 概念辨析 + 脚手架干扰项
│   │   └── distill.py       # 误区提炼（错题/脚手架原料 → 知识化误区，导出期调用）
│   ├── state.py             # AgentState + agent 间消息 Pydantic 模型
│   ├── graph.py             # build_teach_graph 教学子图（依赖注入装配）
│   ├── mastery.py           # 掌握度数学唯一事实源（纯函数）
│   ├── plan.py              # 课程切片（纯函数）+ KnowledgeEntry 模型
│   ├── assess.py            # 确定性出题/判分（纯函数，fail-closed）
│   ├── answer_pipeline.py   # 作答处理管线（CLI/Web 共用入口）
│   ├── session.py           # SessionStore：会话/事件/轮次/资源包 DB 读写（W1）
│   ├── teach_loop.py        # 会话编排服务，CLI/Web 共用（W1，自 run_cli 抽取）
│   ├── deliver.py           # 资源包组装：三形态 + 溯源 + 进阶标记（W1）
│   ├── llm.py               # LLMProvider（chat/chat_validated/chat_stream）
│   ├── retrieval.py         # Retriever：FTS5(CJK) + sqlite-vec + RRF
│   ├── embedding.py         # BGEEncoder（仅组合根 import）
│   ├── config.py            # Settings：唯一 env 读取点
│   └── logging.py           # structlog JSON 配置
├── scripts/                 # 组合根
│   ├── cli_input.py         # CLI 输入净化（开发自测用）
│   ├── init_db.py           # 幂等知识库 loader + 会话表迁移
│   ├── export_packages.py   # 资源包条目化导出（产出物 → 同构 entries.jsonl）
│   └── run_cli.py           # 会话 CLI（薄壳，调 core/teach_loop）
├── api/                     # FastAPI 薄层：序列化→转发→推流，不写业务
│   ├── models.py            # API schema（Pydantic）
│   ├── main.py              # app 工厂 + 启动时常驻装配（provider/encoder/graph）
│   └── routes/              # sessions / teach / question / answers / report / replay
├── web/                     # Next.js 15 App Router（W2 三画面已上线）
│   ├── app/                 # /（画像表单）/ sessions（列表）/ sessions/[id]（学生面工作台）
│   │                        # / sessions/[id]/orchestration（裁判面 live/replay）/ sessions/[id]/report（报告）
│   ├── components/
│   │   ├── ui/              # shadcn/ui（禁止手动修改）
│   │   ├── student/         # 学生面（表单/工作台/教学面板/答题卡）
│   │   ├── orchestration/   # 裁判面（React Flow 画布/溯源侧栏）
│   │   ├── report/          # 报告页（Recharts 图表/资源包浏览）
│   │   └── shared/          # 跨画面复用（badge 等）
│   └── lib/                 # api.ts（REST）/ sse.ts（POST 流解析+GET 订阅）/ types.ts（事件+响应 SSOT）
│                            # / orchestration-reducer.ts（事件→节点状态纯函数）
├── data/
│   ├── seeds/<domain>/entries.jsonl  # 知识条目（一等数据文件，换目录即换领域）
│   ├── seeds/profiles/*.json         # 学习者画像种子
│   ├── knowledge.db         # SQLite（知识库+FTS5+vec+会话+资源包，单文件）
│   └── exports/              # 产出物导出区（entries.jsonl 同构条目——交付物性质，非运行态存储）
├── evals/
│   ├── metrics.py           # 三指标口径 SSOT（与 pytest 同源）
│   ├── profiles/            # 50 组评测画像（3 手工 + 47 程序生成）
│   └── run.py               # 批量评测入口（W2）
├── tests/                   # pytest
├── docs/                    # 赛题 / PRD / 架构 / 选型 / 规范 / 路线图 / 术语表
├── AGENTS.md                # AI agent 精简约束
├── docker-compose.yml       # api + web + db-init（W1 骨架）
└── pyproject.toml
```

**约束**：
- `web/src/components/ui/` 的文件**禁止手动修改**（均为 shadcn CLI 生成，靠 shadcn MCP 更新）；
- 每个 Agent 一个文件，禁止把所有 Agent 塑进同一个文件；
- `core/` 禁止 import `api/`；`api/` 可 import `core/`；依赖方向永远指向 core；
- `core/` 任何模块禁止 import 时产生副作用（建 client、加载模型、读 env）——env 只在 `core/config.py`，资源只在组合根创建；
- 会话运行数据只写 SQLite（sessions/events/rounds/snapshots/packages），禁止散落 JSON 文件做数据存储（评测产出 JSON 属交付物，不算运行态存储）。

---

## 4. 架构约束

### 4.1 信息流方向（最重要的约束）

```text
api/  ←── SSE ──→ web/
  |                 
  v                 
core/  ── state 流 ──
  |
  v
data/  (SQLite)
                          LLM API (外部)
```

- `core/` 不接触网络层（FastAPI/SSE），只操作 state 和调 LLM provider；
- `api/` 是薄层，不做业务逻辑，只做序列化 → 转发 → 推流；
- `web/` 不写业务逻辑，只消费 SSE 和渲染；
- 数据存储：只有 `data/knowledge.db` 一个文件，禁止散落 JSON 配置文件做数据存储；

### 4.2 Agent 上下文隔离规则（防幻觉架构的代码级约束）

- **诊断 Agent**：state 只注入 `{learner_profile, test_results}`，禁止注入知识库条目原文；
- **生成 Agent**：state 注入 `{retrieved_entries, profile_summary, outline, last_review_feedback}`；须在 LangGraph state 定义中用 `Annotated` 标注仅通过检索获取的 key；
- **审核 Agent**：state 注入 `{draft, cited_entries}`——**禁止注入 learner_profile 的任何字段**（这一隔离是防幻觉机制的核心，也是文档创新性部分的关键论点）；
- **对话日志仅用于画像更新**，永不注入任何 Agent 的生成上下文。

### 4.3 图像数据库约束

- 图编译一次，运行时只调用图形实例；禁止每次请求编译新图；
- 每个节点必须设置 `max_tokens` 上限（via prompt trimming 或 provider config）；
- 辩论轮次硬上限 = 3，超过后走降级策略（仅保留已通过论断重组）。

---

## 5. 编码规范

### 5.1 Python（core/ + api/ + evals/）

```python
# ✅ 必须
Python 3.11+
所有 agent 间消息使用 Pydantic BaseModel 或 TypedDict
LangGraph StateGraph 使用 Annotated 定义 reducer
LLM 调用统一走 provider 抽象层，禁止直调 openai.ChatCompletion
日志使用 structlog，输出 JSON 格式（供 SSE 解析）
错误处理：自定义异常类 + FastAPI exception handler 统一兜底

# ❌ 禁止
裸 dict 做 agent 间通信
print() 做日志
裸 try/except 吞异常
在 core/ 中调 openai 或任何 HTTP client 直连
使用 threading（用 asyncio）
```

**参考模式：Pydantic 强制类型**

```python
# core/state.py
from typing import Annotated, TypedDict
from langgraph.graph.message import add_messages

class ReviewNote(BaseModel):
    claim_index: int
    verdict: Literal["supported", "partially_supported", "unsupported"]
    reason: str
    suggestion: Optional[str] = None

class AgentState(TypedDict, total=False):
    learner_id: str
    profile: dict
    gaps: list[str]
    retrieved: list[dict]
    draft: list[dict]            # 生成稿：论断列表
    review_round: int            # 辩论轮次
    review_history: list[ReviewNote]  # 辩论链
    final_resources: dict
    assessment_results: dict
```

### 5.2 TypeScript（web/）

```text
# ✅ 必须
TypeScript strict（tsconfig strict: true），禁止 any
use client 仅用于交互组件（onClick、useState、SSE 订阅）
服务端组件优先（async RSC 做数据初载）
shadcn 组件必须从 @/components/ui 导入，禁止绕过
Tailwind class 无序 → class 用 cn() helper 合并

# ❌ 禁止
any 类型
useEffect + fetch（使用 TanStack Query 或 RSC async）
内联样式 / CSS Module（Tailwind 独占）
在服务端组件中写 onClick
直接 import React Flow 的默认导出（用 @xyflow/react）
```

### 5.3 通用

- 所有文件使用 UTF-8 编码；
- 代码注释：只在非显而易见处加（为什么要这样做，不是做了什么）；API 文档用 docstring；
- 禁止注释掉的代码块——直接删除，git 历史可追溯。

---

## 6. 命名规范

| 元素 | 规范 | 示例 |
|---|---|---|
| Python 文件 | snake_case | `diagnose.py`, `sse.py` |
| Python 类 | PascalCase | `AgentState`, `ReviewNote` |
| Python 函数/变量 | snake_case | `build_graph()`, `learner_profile` |
| TypeScript 文件 | kebab-case | `orchestration-graph.tsx` |
| TypeScript 组件 | PascalCase | `OrchestrationGraph` |
| TypeScript 函数/变量 | camelCase | `useSSEStream()` |
| 数据库表 | snake_case | `knowledge_entries`, `learner_logs` |
| Agent 名 | 英文名，与图节点名一致 | `diagnose`, `generate`, `review` |
| git branch | `type/description` | `feat/diagnose-agent`, `fix/nli-prompt` |

---

## 7. Git 规范

```text
# 分支
main           # 受保护，只通过 PR 合并
feat/*         # 功能分支
fix/*          # 修复分支

# 提交信息
<type>: <short description>

type:
  feat     # 新功能
  fix      # 修复
  refactor # 重构（不改变行为）
  docs     # 仅文档
  eval     # 评测脚本或数据
  infra    # 基础设施（docker/compose/deps）

示例：
  feat: add diagnose agent with profile modeling
  fix: nli prompt missing evidence_ids in context
  eval: add 50 profile test suite
  infra: docker compose with sqlite volume
```

- **禁止** commit `.env`、API key、密钥文件（已在 `.gitignore` 中）；
- **禁止** force push 到 main；
- **禁止** commit shadcn/ui 的自动生成文件后再手动修改（要么不 commit 要么让 shadcn MCP 管理）。

---

## 8. AI 开发协作约定

本节专门约束 AI agent（opencode / Claude Code / Cursor）的生成行为。违反本节约束的代码会被要求重写。

### 8.1 必须遵守

1. **严格按照本仓库目录结构生成代码**：新文件必须放在对应目录，禁止在项目根目录创建源码文件；
2. **新依赖必须先问再装**：任何 `pip install` / `pnpm add` 之前必须在对话中说明理由并等待确认（已在禁止清单里的直接驳回）；
3. **错误必须暴露，禁止静默降级**：try/except 必须记录日志 + 返回结构化错误，禁止 `pass`；
4. **图编译一次，运行时复用**：禁止每次请求 new StateGraph + compile；
5. **生成 Agent 输出必须挂 evidence_ids**：输出 schema 中每条论断的 `evidence_ids` 为必填字段；
6. **审核 Agent 上下文不得包含 learner_profile**：代码审查检查项。

### 8.2 建议遵守

- 新文件写好 → 立刻写对应的 pytest，不要"稍后补"；
- TS 端先定义类型再写组件（TypeScript 的 `interface` 或 Zod schema）；
- Python 端先定义 Pydantic model 再写函数。

### 8.3 AI 常见错误模式（见到以下模式直接拒绝）

| 错误模式 | 说明 |
|---|---|
| 一上来就写 docker compose + Redis + Worker | 过度架构，参考 §4.3 模块化单体 |
| 导入 `@anthropic-ai/sdk` 或 `openai` 然后直调 | 必须走 provider 抽象层 |
| 创建 `server/` 目录放 Express | 本项目只有 `api/`(FastAPI) |
| 写 `useEffect(() => { fetch(...).then(...) }, [])` | TanStack Query 或 RSC 替代 |
| 在 core/ 里导入了 `fastapi` 或 `starlette` | 依赖方向错误 |
| 新装一个向量库替代 sqlite-vec | 已在禁止清单 |
| 把 3 个 Agent 都写在一个 `.py` 文件里 | 一个 agent 一个文件 |
| 给审核 Agent 注入了 learning_profile | 违反上下文隔离 |

---

## 9. 环境与运行命令

### 9.1 启动开发环境

```bash
# Python 依赖
uv sync

# 初始化知识库 + 会话表（幂等，可反复运行）
uv run python scripts/init_db.py

# CLI 会话（开发自测）
uv run python scripts/run_cli.py test1              # 手动作答
uv run python scripts/run_cli.py test1 --sim 0.8    # 模拟学生（0.8 概率答对）

# API（W1 起可用）
uv run uvicorn api.main:app --reload --port 8000

# 前端（三画面已上线）
cd web && pnpm install && pnpm dev
```

### 9.2 常用命令

```bash
# 测试 / 类型 / lint（三者全过才算任务完成）
uv run pytest -v
uv run pyright core/ scripts/ tests/ api/ evals/
uv run ruff check .

# 前端检查
cd web && pnpm typecheck && pnpm lint

# 批量评测（W2 起，产出三指标 JSON 报告）
uv run python evals/run.py

# Docker 一键起（交付）
docker compose up --build
```

### 9.3 环境变量（模板见 .env.example）

```bash
LLM_BASE_URL=https://api.openai.com/v1   # 任意 OpenAI 兼容接口
LLM_API_KEY=sk-xxx
LLM_MODEL=gpt-4o-mini
# DIAGNOSE_MODEL= 等：各 Agent 独立模型（可选）
# LLM_EXTRA_BODY={"thinking": {"type": "disabled"}}  # deepseek 系务必配置（见 AGENTS.md 坑表）
HF_ENDPOINT=https://hf-mirror.com        # 国内镜像
DATABASE_PATH=data/knowledge.db
BGE_MODEL_PATH=data/bge-m3
```

---

## 10. 附录：关键参考模式

> 本节为**真实代码的索引**而非重写——实现以源文件为准，此处标注位置与要点（完整设计见 `架构设计文档.md`）。

| 模式 | 位置 | 要点 |
|---|---|---|
| Provider 抽象层 | `core/llm.py` | AsyncOpenAI 显式 Timeout(connect=10, read=180)；chat_validated 校验重试带错误详情；chat_stream 逐 token；surrogate 双侧净化；finish_reason=length 抛 LLMOutputError |
| SSE 推送 | `api/routes/`（W1） | SSE 帧 = session 事件 JSON + `event:` 类型行；实时与回放同构 |
| 混合检索 | `core/retrieval.py` | FTS5(CJK 逐字切分) + sqlite-vec + RRF；三处分词必须同语义（坑表） |
| 图装配 | `core/graph.py` | build_teach_graph(settings, provider, retriever) 依赖注入；组合根编译一次持有复用 |
| 作答管线 | `core/answer_pipeline.py` | 规则预筛 → LLM 复核 → missed 硬降级 → fail-closed；CLI/Web 共一入口 |
| 掌握度数学 | `core/mastery.py` | recency 加权 + 置信度封顶 + 门槛 0.7 + 连错降维；纯函数唯一事实源 |
