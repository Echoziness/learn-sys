# 架构设计文档

> **版本**：v1.3 · 2026-08-26
> **文档类型**：Architecture — 系统怎么实现的唯一事实源。v1.1 of ADR 拆分时引用的本文档曾遗失，本版重建并以此为准。
> **阅读顺序**：技术选型见 `技术选型记录.md`（Why）→ 本文（What/How）→ `开发约束与工程规范.md`（Rule）。

---

## 1. 总览：四层模型

```
┌─────────────────────────────────────────────────────────────┐
│ 决策可视化层   web/ 裁判面（React Flow）+ 学生面 + 报告页      │ ← 体验/创新分载体
├─────────────────────────────────────────────────────────────┤
│ 会话服务层     api/（FastAPI 薄层）+ core/teach_loop 编排      │ ← 事件流生产/消费
├─────────────────────────────────────────────────────────────┤
│ 教学内核层     core/ agents + 纯函数（assess/mastery/plan）    │ ← 已完成，冻结打磨
│               + answer_pipeline + deliver（资源组装）         │
├─────────────────────────────────────────────────────────────┤
│ 数据层         SQLite 单文件（知识库 + 会话 + 资源包 + 事件流） │
└─────────────────────────────────────────────────────────────┘
```

运行态组件恒定：一个 Python 进程（api）+ 一个 Node 进程（web）+ 一个 SQLite 文件。

**依赖方向**：`web/ ─(SSE)→ api/ → core/ → data/`。core/ 不接触网络层；api/ 只做序列化→转发→推流；图在组合根编译一次。

## 2. 模块划分（core/ 扩展后全景）

| 模块 | 职责 | 状态 |
|---|---|---|
| `core/agents/*` | 七个 LLM/规则节点（见 PRD §4，含 distill 误区提炼） | ✅ 冻结 |
| `core/assess.py` `core/mastery.py` `core/plan.py` | 出题/掌握度/切片纯函数 | ✅ 冻结 |
| `core/answer_pipeline.py` | 作答处理共用服务（CLI/Web 同入口） | ✅ |
| `core/graph.py` | 教学子图装配（retrieve→generate→review） | ✅ |
| `core/session.py` | SessionStore——会话/事件/轮次/掌握度/资源包的 DB 读写 + 事件发射 | ✅ |
| `core/teach_loop.py` | 会话编排服务（诊断→切片→逐主题循环），CLI 与 Web 共用 | ✅ |
| `core/deliver.py` | 资源包组装（纯函数：claims→讲义、题目归档、指南提取、进阶标记、条目化导出 package_to_entry） | ✅ |
| `core/retrieval.py` `core/embedding.py` `core/llm.py` `core/config.py` `core/logging.py` | 基础设施 | ✅ |
| `scripts/export_packages.py` | 产出物复用管线：资源包 → 知识库同构 entries.jsonl（distill 提炼 + 三重自检） | ✅ |

**关键决策**：事件发射不进 agent 节点签名。节点保持 `(state, *, provider, model)` 不变；编排层（teach_loop）与 api 层消费 LangGraph `astream(stream_mode="updates")` 转换为 session 事件——core 内核零改动，CLI/Web 天然一致。

## 3. 数据模型（SQLite，全部幂等迁移）

### 3.1 既有表（不动）

`knowledge_entries`（含 FTS5/vec 外部表）、`learner_profiles`（种子画像，只读）。

### 3.2 新增表（W1，`scripts/init_db.py` 幂等迁移）

```sql
-- 会话元数据：画像快照 + 诊断结果 + 切片
CREATE TABLE IF NOT EXISTS sessions (
  session_id TEXT PRIMARY KEY,          -- uuid4hex
  learner_id TEXT NOT NULL,
  domain TEXT NOT NULL DEFAULT 'bigdata-analysis', -- 教学领域（seeds 子目录，多域选择，2026-08-28）
  profile_json TEXT NOT NULL,           -- 输入画像完整快照
  gap_ids_json TEXT,                    -- 诊断收敛的本体条目 ID
  difficulty_level TEXT,                -- beginner/intermediate/advanced
  profile_summary TEXT,
  plan_json TEXT,                       -- 切片结果（含前置链）
  status TEXT NOT NULL DEFAULT 'active',-- active / finished / aborted
  created_at TEXT NOT NULL,
  finished_at TEXT
);

-- 统一事件流：裁判面渲染协议 + 回放媒体流 + 审计日志（一表三用）
CREATE TABLE IF NOT EXISTS session_events (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  session_id TEXT NOT NULL,
  seq INTEGER NOT NULL,                 -- 会话内单调递增，回放排序键
  event_type TEXT NOT NULL,             -- 见 §4 事件协议
  payload_json TEXT NOT NULL,           -- 自包含：前端仅凭 payload 可渲染
  created_at TEXT NOT NULL
);
CREATE INDEX IF NOT EXISTS idx_events_session ON session_events(session_id, seq);

-- 教学轮快照：题目/作答/判分/决策的结构化中间数据（提交物"协同决策中间数据"）
CREATE TABLE IF NOT EXISTS topic_rounds (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  session_id TEXT NOT NULL,
  entry_id TEXT NOT NULL,
  round_no INTEGER NOT NULL,
  question_json TEXT,                   -- 题目（含题型、题干、选项；expected 单列）
  expected_json TEXT,                   -- 判分要点（不进学生视野）
  answer_text TEXT,
  grade_json TEXT,                      -- 规则覆盖率 + LLM verdict + evaluation + missed
  decision TEXT,                        -- advance / retry / regress / scaffold
  mastery_after REAL,
  created_at TEXT NOT NULL
);

-- 掌握度历史：报告曲线与跨会话延续
CREATE TABLE IF NOT EXISTS mastery_snapshots (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  session_id TEXT NOT NULL,
  learner_id TEXT NOT NULL,
  entry_id TEXT NOT NULL,
  round_no INTEGER NOT NULL,
  correctness INTEGER NOT NULL,         -- 0/1（脚手架答对不写入）
  mastery_after REAL NOT NULL,
  created_at TEXT NOT NULL
);

-- 资源包：三形态资源 + 溯源链（赛题主交付物）
CREATE TABLE IF NOT EXISTS resource_packages (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  session_id TEXT NOT NULL,
  learner_id TEXT NOT NULL,
  entry_id TEXT NOT NULL,
  lecture_json TEXT,                    -- 讲义：审核通过论断 [{text, evidence_ids, claim_type, round}]
  questions_json TEXT,                  -- 分阶题：[{type: choice/scaffold/answer, prompt, options?, round}]
  practice_json TEXT,                   -- 实操指南：{steps: [{text, evidence_ids}], checkpoints}（procedure 条目）
  challenge_json TEXT,                  -- 进阶挑战任务（mastery≥0.85 时）
  difficulty_tier TEXT NOT NULL,        -- 资源难度层级（适配率指标的判定输入）
  created_at TEXT NOT NULL,
  UNIQUE(session_id, entry_id)
);

-- 条目化导出产物：与知识库同构的知识条目（产出物可复用的本体，FR-23）
CREATE TABLE IF NOT EXISTS exported_entries (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  session_id TEXT NOT NULL,
  entry_id TEXT NOT NULL,               -- 生成条目 id（GEN-<源条目>-<学习者>）
  source_entry_id TEXT NOT NULL DEFAULT '', -- 源知识库条目 id（报告页溯源）
  entry_json TEXT NOT NULL,             -- SeedEntry 同构完整条目（可被 init_db 原样入库）
  exported_at TEXT NOT NULL,
  UNIQUE(session_id, entry_id)          -- 重导出覆盖同 id 条目
);
```

**不变量**：画像/本体只读（种子文件是事实源）；会话与产物可整体清理（DELETE 端点，事件/轮次/掌握度必删，资源包与导出条目可额外保留为孤儿行——溯源字段已在行内，资源库照常聚合）；DB 变更一律幂等迁移，禁止删库重建。

## 4. 事件协议（裁判面与回放的渲染指令集）

事件 = `(seq, event_type, payload)`，payload 自包含。类型清单：

| event_type | payload 要点 | 消费者 |
|---|---|---|
| `session_start` | 画像、learner_id | 学生面、裁判面 |
| `diagnose_done` | gap_ids、gaps 文本、difficulty_level、summary | 双面 |
| `plan_done` | 切片列表 [{entry_id, title, 序位}] | 双面（路径图初值） |
| `topic_start` | entry_id、round_no、retry 轮标记 | 双面 |
| `retrieve_done` | queries、命中条目 [{id,title,score}]、uncovered | 裁判面（检索面板） |
| `generate_done` | claims 数、cited_entries | 裁判面（节点脉冲） |
| `review_done` | 逐条裁决 [{claim_index, verdict, reason}]、unsupported 数、轮次 | 裁判面（打回回路主画面） |
| `teach_delivered` | 最终论断全文（含 evidence_ids、claim_type） | 学生面（讲义正文）、裁判面（溯源侧栏） |
| `question_built` | 题型、题干、选项（无 expected） | 学生面出题卡 |
| `answer_graded` | 规则覆盖率、verdict、evaluation、missed_requirements、decision、mastery_after | 双面（反馈卡 + 决策节点） |
| `scaffold_offered` | 题干、镜像干扰项来源说明 | 双面 |
| `followup_asked` | entry_id、round_no、student_question、valid、reason、answer（有效时携带困惑解答，已记录待下轮教学消化） | 双面（追问面板 + 裁判面判定节点） |
| `followup_offered` | （历史事件，仅旧会话回放兼容，不再产生）确认型选择题三件套 | 学生面追问面板 |
| `followup_graded` | （历史事件，仅旧会话回放兼容，不再产生）确认题判分 | 双面 |
| `topic_advance` / `topic_regress` | entry_id、目标条目、原因（连错/达标） | 裁判面（路径图推进/回边） |
| `package_saved` | entry_id、三形态资源就绪标记、进阶挑战标记 | 报告页、资源总览 |
| `packages_exported` | entry_ids、count（条目化导出落库后发，审计用） | 裁判面、回放 |
| `session_end` | 汇总统计（主题数、轮次、最终 mastery 汇总） | 报告页 |

**实时与回放同构**：实时会话中 api 边执行边写 `session_events` 并推 SSE；回放模式 `GET /replay` 按 seq 重放同一流，前端渲染代码完全复用——评委无 LLM key 环境的可演示保障。

## 5. API 契约（薄层，业务全部在 core）

| 端点 | 方法 | 职责 |
|---|---|---|
| `/api/sessions` | POST | 建会话：画像 + 可选 domain（默认 bigdata-analysis）→ 诊断 + 切片 → 持久化，返回 session_id + plan |
| `/api/sessions/domains` | GET | 可选教学领域列表（seeds 子目录 + 条目数）——新建会话表单领域下拉数据源（2026-08-28） |
| `/api/sessions` | GET | 会话列表（回放入口页）：learner/状态/主题数/事件数/资源包数 |
| `/api/sessions/{id}` | GET | 会话详情（画像快照 + 诊断 + 切片） |
| `/api/sessions/{id}/topics/{entry}/teach` | POST | 执行教学子图，SSE 推 §4 教学事件，终帧为 teach_delivered |
| `/api/sessions/{id}/topics/{entry}/question` | POST | 出题（choice 确定性 / answer 走 question 节点），JSON 响应 |
| `/api/sessions/{id}/topics/{entry}/state` | GET | 主题进度状态：needs_teaching（巩固模式；有未消化困惑时强制 true）/ scaffold_pending / prereq_id / followup_last（最近困惑记录，刷新恢复）/ pending_followup_count——Web 工作台镜像 CLI 状态机与刷新恢复的依据 |
| `/api/sessions/{id}/answers` | POST | 作答 → answer_pipeline 判分 → 决策 + 资源沉淀，JSON 响应（verdict/evaluation/missed/decision/mastery） |
| `/api/sessions/{id}/report` | GET | 报告数据：盲区雷达 / 难度匹配（含逐包层级明细）/ 路径（含回边）/ 赛题三指标总览（口径同 evals/metrics.py SSOT） |
| `/api/sessions/{id}/resources` | GET | 资源包列表（三形态 + 溯源链） |
| `/api/sessions/{id}/topics/{entry}/followup` | POST | 动态追问：判定有效性 → 有效则记录困惑并直接给出解答（不即时出题；解答锚定条目+讲义，同域校验失败判无效）；困惑注入下一轮教学（`followup_context`）并作为 distill 原料 |
| `/api/sessions/{id}/exports` | GET | 条目化导出产物（知识库同构条目，由导出管线产出后落库；未导出时返回空数组） |
| `/api/sessions/{id}/export` | POST | 主动触发条目化导出（收集→distill 提炼→自检→落库→packages_exported；409 无资源包 / 502 自检或执行失败） |
| `/api/sessions/{id}/export/download` | GET | 导出文件下载（SeedEntry 同构 jsonl，Content-Disposition attachment；会话已删的孤儿产物亦可下载） |
| `/api/sessions/{id}` | DELETE | 删除会话与过程数据（事件/轮次/掌握度）；`?keep_packages&keep_exports` 额外保留资源包/导出条目（孤儿行，仍在资源库聚合） |
| `/api/resources` | GET | 跨会话聚合资源库（`?session_id&entry_id` 筛选）：返回 {packages, exports}；LEFT JOIN 会话，保留的孤儿产物 session_status=null |
| `/api/sessions/{id}/stream` | GET | 实时事件订阅（SSE）：先 subscribe 再补读历史、按 seq 去重——裁判面 live 模式跟随另一 tab |
| `/api/sessions/{id}/replay` | GET | 历史事件流重放：`?format=json` 返回带 seq 数组（播放器步进用）；默认 SSE 流 |
| `/api/sessions/{id}/end` | POST | 结束会话：发 session_end 汇总事件并落状态 |

约束：请求/响应 Pydantic 模型在 `api/models.py`；SSE 帧 = JSON 序列化的 session 事件 + `event:` 类型行；每端点有 exception handler 兜底（LLM 失败返回结构化错误事件，不静默）。

**运行时装配**：api 启动时创建 provider / encoder / retriever / 编译图并常驻（BGE-M3 约 2GB，进程生命周期内只加载一次）；知识条目**按域分组加载**（`entries_by_domain`，TeachLoop 按会话 domain 取域内条目）；会话状态从 DB 重建，api 无内存会话态。
**多域同域隔离**（2026-08-28）：检索两路（FTS/向量）均按会话 domain 过滤，诊断目录/切片/教学/资源全链路同域；领域间无前置依赖（前置闭包按域计算）。换领域 = 新增 `data/seeds/<域>/entries.jsonl` + init_db，零代码。

## 6. 会话状态机（教学轮）

```
topic_start ─→ teach（子图：retrieve→generate→review，定向打回，审核≤2 轮）
            ─→ question（choice | scaffold | answer，floor 单向推进）
            ─→ answer ─→ grade（规则预筛 → LLM 复核 → 题意核对硬收口）
                     ├─ advance ─→ package_saved ─→ 下一 topic（或 session_end）
                     ├─ retry   ─→ topic_start(round+1, retry_context 注入)
                     ├─ regress ─→ 前置 topic 的 topic_start
                     └─ scaffold_pending ─→ question(scaffold) ─→ 回 answer
```

决策由 `core/mastery.decide_next_step` 纯函数裁决；编排状态机在 `core/teach_loop.py`，CLI 与 Web 共用。

## 7. 上下文隔离矩阵（agent 读写权限 SSOT）

唯一事实源为 `AGENTS.md` §3.5 节点表（diagnose/retrieve/generate/review/question/feedback/answer_pipeline 的读写 key 清单）。红线重申：review 零画像字段；generate 只读 profile_summary；对话日志永不进生成上下文；expected 永不进学生视野（裁判面可见——审计视角）。越界即 code review 驳回。

## 8. 关键机制实现索引

| 机制 | 位置 | 一句话 |
|---|---|---|
| 分层审核 | `core/agents/review.py` | core 逐字支持 / extension 概念一致；merge_verdicts 纯函数 unsupported 优先 |
| 判分级联 | `core/answer_pipeline.py` | 规则预筛 → LLM 复核 → missed 硬降级 → fail-closed |
| 题型阶梯 | `core/assess.py` | 掌握度驱动 + floor_type 单向推进 + 脚手架状态机 |
| 深度契约 | `core/agents/question.py` | taught_claims 为出题上限；retry 缓存失效 |
| 教学弧 | `core/agents/generate.py` | 概念(定义+类比)→示例(worked example)→要点(条目原文写明的规则)；choice 为概念辨析三件套 |
| 资源组装 | `core/deliver.py`（W1） | 审核通过论断 + 归档题目 + 指南提取 + 进阶标记 |
| 条目化导出 | `core/export_pipeline.py`（2026-08-26 下沉） | 资源包 → SeedEntry 同构条目；讲义论断经导出三重过滤（剔 extension/剔学习者指涉/跨轮去重）+ distill 误区提炼；四重自检后双写（文件交付物 + exported_entries 表），init_db 可原样入库 |
| 追问困惑回流 | `core/teach_loop.py` + `core/agents/question.py`（2026-08-28 设计回归） | 提问→判定→落困惑记录并直接解答（锚定校验通过即有效）→下轮注入 `followup_context`（错因回流同通道）→消化从历史推导；困惑进 distill 原料 |
| 事件流 | `core/session.py`（W1） | 写 DB + SSE 订阅推送；回放同构 |
| 回放 | `api/routes/` | 按 seq 重放 session_events |

## 9. 部署形态

本地部署（2026-08-27 放弃 Docker：赛题无容器要求，本地部署失败面更小）：两个进程 + 一个 SQLite 文件——`api`（uvicorn + BGE 常驻，init_db 幂等迁移由启动前单独执行）与 `web`（Next.js）；模型随 `data/bge-m3/` 携带，`HF_HUB_OFFLINE=1` 全离线。步骤与验收见 `docs/部署说明.md`；提交包由 `scripts/pack_delivery.py` 生成。LLM key 通过 `.env` 注入；无 key 环境走回放模式演示。

---

> **变更记录**
> - v1.8 (2026-08-28)：多域教学落地——sessions 加 domain 列（幂等迁移）、api 按域分组加载条目、检索两路同域过滤、`/api/sessions/domains` 端点 + 建会话 domain 参数（默认域向后兼容）、state 端点增 followup_last / pending_followup_count；知识库双域并存（bigdata-analysis 31 + linux-ops 18）。
> - v1.7 (2026-08-28)：动态追问设计回归——从"即时生成确认题"改回原始设计意图"记录困惑→直接解答→下一轮教学注入（`followup_context`，错因回流同通道）→进 distill 原料"；移除 `/followup/answer` 端点与 followup_offered/graded 事件（仅历史回放兼容）；有未消化困惑时 needs_teaching 强制 true（巩固模式让位）。
> - v1.6 (2026-08-26)：幻觉防控三处机制修正——①裁决语义对齐赛题幻觉本义（幻觉=专业知识谬误）：与条目矛盾/编造才判 unsupported 计入幻觉，条目未写明但事实正确的领域常识判 partially_supported（不计幻觉），讲义仍只收 supported；②审核回流改定向打回：阈值从 ≥2 降为任意 1 条，`rejected_claims` 通道只重写被驳回论断；③裁决日志模型（`latest_verdicts`）：review_history 是 append-only 裁决日志，每轮只 append 本轮新裁决的论断，论断当前裁决 = 日志中最新一条（闸门/事件/指标同源）——裁决属于论断不属于轮，rewrite 轮只复审被改写论断，未改写论断裁决天然不漂移（LLM 裁决轮间非确定，整稿复审会翻判——p05 实测 round1 通过的 3 条在 round2 翻判，驳回 2 条变 4 条），修复后同画像幻觉率 6.6%→0%（单会话 6 次打回全部定向修复、零翻判）。
> - v1.5 (2026-08-26)：收盘四项改进——①适配率口径科学化（容忍带 = 层级上限+1，见 core/deliver）；②报告页打磨：难度匹配卡去误导图表改逐包层级徽章明细、路径图加图例、新增三指标总览（口径走 evals/metrics.py SSOT）；③动态追问（侧车轮）：新增三个追问事件与两端点，与错题→脚手架同构、不写掌握度；④导出主动触发：管线下沉 `core/export_pipeline.py`（脚本薄壳），新增触发/下载端点，报告页主入口 + 资源库次入口。
> - v1.4 (2026-08-26)：会话清理与资源库——`DELETE /{id}`（过程数据必删、产物可保留）+ `GET /api/resources`（跨会话聚合，会话/条目双筛选）；前端新增 /resources 资源库页面与会话列表删除确认，共享组件抽至 `shared/resource-views`；报告页雷达图以门槛 0.7 为视觉中点（展示层映射，Tooltip 仍原值）。
> - v1.3 (2026-08-26)：条目化导出产物落库——新增 `exported_entries` 表 + `GET /{id}/exports` 端点 + `packages_exported` 事件；报告页展示知识库同构条目本体（含实操指南 steps 溯源结构修正）。
> - v1.2 (2026-08-23)：教学质量弧（generate 三层论断 + choice 概念辨析 + distill 误区提炼 agent）；新增产出物复用管线 `scripts/export_packages.py`（资源包 → 知识库同构条目，可复用闭环）。
> - v1.1 (2026-08-17)：W2 落地同步——API 契约表补全 12 端点（新增列表/stream 实时订阅/replay json/end/topic state；teach/question 响应形态修正）；core 三模块状态转 ✅；教学/出题路径以本文档为准（曾出现实现与契约不一致被 E2E 抓出，已修）。
> - v1.0 (2026-08-14)：重建（ADR v1.1 引用的文档曾遗失）。定稿四层模型、5 张新表、事件协议 14 类、API 七端点、教学轮状态机；core 扩展三模块（session/teach_loop/deliver）。
